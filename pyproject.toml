[project]
name = "budget"
version = "1.0.0"
requires-python = ">=3.12"
license = { text = "MIT" }
dependencies = [
  "PySide6>=6.6",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
